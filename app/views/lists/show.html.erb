<div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://images.unsplash.com/photo-1594908900066-3f47337549d8?ixlib=rb-1.2.1&ixid=MnwxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8&auto=format&fit=crop&w=1740&q=80)">
    Welcome to <%= @list.name%> list
</div>

<div class="container mb-5">
  <div class="d-flex justify-content-between my-4">
    <span class="mb-2 d-block"><small><em class="text-muted"><%= "You haven't saved any movies yet!" if @list.movies.empty? %></em></small></span>
    <%= link_to "delete this list", list_path(@list), method: :delete, class: "btn btn-dark m-3", data: { confirm: "Are you sure you want to delete this list?" } %>
  </div>
  <div class="bg-white rounded shadow h-400">
    <%= simple_form_for [@list, @bookmark] do |f| %>
      <div class="p-3 rounded-bottom d-flex flex-column">
        <%= f.input :comment %>
        <%= f.association :movie%>
        <%= f.submit class:"btn btn-dark m-3" %>
      </div>
    <% end %>
  </div>
  <div class="row my-2">
    <% @list.movies.each do |movie| %>
      <% bookmark = Bookmark.find_by(list: @list, movie: movie) %>
      <div class="col-12 col-sm-6 mb-4" data-aos="zoom-in">
        <div class="bg-white rounded shadow h-100">
          <%= image_tag movie.poster_url, alt: movie.title, class: "movie-img rounded-top" %>
          <div class="p-3 rounded-bottom d-flex flex-column">
            <strong><%= movie.title %> <span class="text-muted font-weight-normal">• <%= movie.rating %> <i class="fas fa-star star-yellow"></i></span></strong>
            <small class="text-muted"><%= movie.overview %></small>
            <div class="d-flex align-items-end justify-content-between pt-4">
              <div>
                <i class="fas fa-quote-left me-2"></i><small class="flex-grow-1"><%= bookmark.comment %></small>
              </div>
              <small class="text-right flex-shrink-0"><%= link_to "delete", bookmark_path(bookmark), method: :delete, data: { confirm: "Are you sure you want to remove #{movie.title} from your #{@list.name} list"}, class: 'text-danger' %></small>
            </div>
          </div>
        </div>
      </div>
    <% end %>
  </div>
  <div class="row my-2">
   <div class="col-12 col-sm-6">
        <div class="card">
          <div class="card-header bg-dark text-white">
            Reviews
          </div>
          <% @list.reviews.each do |review| %>
            <div class="card-body">
              <blockquote class="blockquote mb-0">
                  <p><%= review.content %></p>
                  <footer class="blockquote-footer"><%= review.rating %> stars</footer>
                </blockquote>
            </div>
          <% end %>
        </div>
    </div>
    <div class="col-12 col-sm-6">
       <div class="bg-white rounded p-3 bg-light">
         <%= simple_form_for [@list, @review] do |f|%>
           <%= f.input :content, placeholder: "Add your review here..." %>
           <%= f.input :rating, as: :select, collection: (1..5), required: true, prompt: "What's your rating", input_html: { data: { controller: "jquery-bar-rating" } } %>
           <%= f.submit class:"btn btn-dark m-3" %>
         <% end %>
       </div>
    </div>
 </div>

 <div class="container mb-5">
  <%= link_to "back to lists", lists_path, class: "btn btn-dark m-3 text-center" %>
 </div>
